/** Skladno s fiksno spodnjo mobilno vrstico (ikone + varnostni pas spodaj). */
export const MOBILE_FOOTER_MAIN_PADDING_BOTTOM =
  "calc(70px + env(safe-area-inset-bottom, 0px))";

/** Fiksni gumbi in messenger dock nad spodnjo vrstico (višina vrstice + varnostni pas + reža). */
export const MOBILE_STACK_ABOVE_FOOTER =
  "calc(82px + env(safe-area-inset-bottom, 0px))";

/** Približna višina fiksnega zgornjega navbarja na mobilnem (px + safe area). */
export const MOBILE_NAV_TOP_OFFSET =
  "calc(68px + env(safe-area-inset-top, 0px))";
